# core

Core structues used throughout the game. This crate doesn't depend on any other crates in the workspace.

Run crate-specific commands with `cargo <cmd> -p core`. For example, run `cargo test -p core` to run the test suite.
