# bevy_interface

An pure Rust desktop interface using [Bevy](https://bevyengine.org).

Run crate-specific commands with `cargo <cmd> -p single_player`. For example, run `cargo run -p bevy_interface` to start a development build. Debugging should be in the terminal or debugger.
